# MOVED

This legacy repo-local `wiki/` path has been decommissioned.

Canonical curated knowledge now lives in:

- `Dev`: `/home/maxim/KnowledgeVault/`
- `Prod`: `/home/hermes/KnowledgeVault/`

Canonical vault-path contract:

1. `PKM_VAULT_PATH`
2. `OBSIDIAN_VAULT_PATH` only as a secondary compatibility fallback
3. fallback default: `$HOME/KnowledgeVault`

Do not write new canonical curated pages into this repo-local directory.
Use the resolved canonical vault path instead.
